// -----------------------------------------------------------------------------
// WORK · EXTERNAL LINK
// A work that links straight to an external URL — no internal portfolio page.
// Only `title`, `year` and `href` are required; add `cover` for the hover
// thumbnail and `status` for a badge. No `blocks`/`media`/`slug` needed.
// -----------------------------------------------------------------------------
export default {
	title: `IPCC taylor's version`,
	year: '2025',
	href: 'https://dianayukari.github.io/ipcctaylorsversion/', // TODO — where the row links to
	cover: '/works/ts-ipcc/cover.gif' // TODO — optional hover thumbnail
};
